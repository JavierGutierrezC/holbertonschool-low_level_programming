#include "holberton.h"
/**
 * _islower - checks for lower case letters.
 * @c: checks to see if char is lower case
 *
 * Return: Always 0
 */

int _islower(int c)
{

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
		if (c >= 'a' && c <= 'z')
		{
			return (1);
		}
	else
		return (0);
}

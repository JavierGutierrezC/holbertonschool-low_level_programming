#include "holberton.h"

/**
 * mul - funtion to multiply digits
 * @a: - digit to multiply
 * @b: - digit to multiply
 * Return: - is the multiplication of the 2 digits a and b
 */

int mul(int a, int b)
{
	int mult = a * b;

	return (mult);
}
